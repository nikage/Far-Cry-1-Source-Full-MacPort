name: minimap_icon_generator
description: Generates Far Cry minimap DDS icons and packs them into a CryPak archive.
version: 0.1.0
environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  args: ^2.4.2
  archive: ^3.6.1
  path: ^1.8.3

dev_dependencies:
  lints: ^2.1.1
  test: ^1.25.2

