name: intro_video_converter
description: Converts Far Cry intro BIK videos into H.264 MP4s and packs them.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.0
  archive: ^4.0.0
  path: ^1.8.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.0

